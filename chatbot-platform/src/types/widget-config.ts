export interface WidgetConfig {
    primaryColor: string;
    secondaryColor: string;
    buttonSize: number;
    windowWidth: number;
    windowHeight: number;
    headerText: string;
    fontFamily: string;
}
